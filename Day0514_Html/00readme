HTML ( HyperText Markup Language )
: 웹 브라우져에서 보이는 화면을 그리기 위한 언어
HyperText - 링크를 통해 다른문서나 사이트로 이동할 수 있는 기능

Tag(태그)로 구성
<태그이름> 내용  </태그이름>
태그에는 속성 값 (class, id, style 등등)이 들어 갈 수 있다.
<태그명 (속성이름1  = "속성 값1") (속성이름2 = "속성 값2")> 

</태그명>

태그를 이용해서 문서를 구분하기 때문에 markup이라고 한다.
(이미지, 버튼, 글자, 제목인지.....)

 
 HTML은 웹 브라우저에 의해 위에서부터 한 줄씩 해석되서 실행됨
 
 <div id = "header"> </div>
 <header>
 	<div>
 	
 	</div>
 </header>
 <nav>
 <article>
 <section>
 <aside>
 <footer>
 목적에 맞는 이름을 가지고 있는 태그를 시멘틱태그라고 한다.
 